# Copy this file to .env.local and fill in your own key.

# Free API key for MapTiler (https://cloud.maptiler.com/account/keys/),
# used for the light/dark map basemap styles in Peta Wilayah. Without this,
# the map still works but light/dark fall back to plain OSM tiles.
VITE_MAPTILER_KEY=
